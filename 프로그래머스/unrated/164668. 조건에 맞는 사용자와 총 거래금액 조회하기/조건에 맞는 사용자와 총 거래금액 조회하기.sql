-- 코드를 입력하세요
SELECT USER_ID, NICKNAME, sum(PRICE) as "TOTAL_SALES" FROM USED_GOODS_BOARD b LEFT JOIN USED_GOODS_USER u on b.WRITER_ID = u.USER_ID WHERE b.STATUS = "DONE" GROUP BY u.USER_ID HAVING sum(PRICE) >= 700000 ORDER BY sum(PRICE) asc;