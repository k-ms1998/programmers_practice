-- 코드를 입력하세요
SELECT b.CATEGORY, sum(s.SALES) as "TOTAL_SALES" from BOOK b left join BOOK_SALES s on b.BOOK_ID = s.BOOK_ID where year(s.SALES_DATE) = 2022 and month(s.SALES_DATE) = 1 group by b.CATEGORY order by b.CATEGORY;