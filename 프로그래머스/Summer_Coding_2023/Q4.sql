SELECT b.ID as "ID", b.NAME as "NAME", ROUND(AVG(SALARY)) as "AVERAGE" FROM BRANCHES b LEFT JOIN EMPLOYEES e1 ON b.ID = e1.BRANCH_ID GROUP BY b.ID HAVING ROUND(AVG(SALARY)) > (SELECT ROUND(avg(SALARY)) FROM EMPLOYEES e2) ORDER BY b.ID ASC;